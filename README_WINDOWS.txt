Windows notes (optional mode):
- This package runs without requesting admin. Blocking is disabled by default.
- To enable blocking later: run elevated PowerShell, activate venv, set AUTO_BLOCK=1, then run run_monitor.py
