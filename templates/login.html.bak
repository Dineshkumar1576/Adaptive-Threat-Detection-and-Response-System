<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Login — Adaptive Threat</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
      body { background: radial-gradient(circle at 10% 10%, #071018, #000000); color: #c8f7ff; height:100vh; display:flex; align-items:center; justify-content:center; font-family: 'Segoe UI', Roboto, sans-serif; }
      .card { width: 360px; background: rgba(10,14,18,0.85); border: 1px solid rgba(0,200,255,0.06); box-shadow: 0 10px 30px rgba(0,0,0,0.6); border-radius:12px; color:#bfefff; }
      .logo { font-family: 'Courier New', monospace; letter-spacing:2px; color:#00f5ff; font-weight:700; }
      .form-control { background: rgba(0,0,0,0.35); border: 1px solid rgba(255,255,255,0.04); color:#e6fbff; }
      .btn-primary { background: linear-gradient(90deg,#00f5ff,#00a3ff); border:none; box-shadow:0 6px 18px rgba(0,160,255,0.12); }
      .small-muted { color:#7fd8ea; opacity:0.8; font-size:0.85rem; }
      .hint { font-size:0.8rem; color:#66e0ff; opacity:0.6; margin-top:8px; }
      .error { background: rgba(255,0,0,0.06); padding:8px; border-radius:6px; color:#ffb3b3; margin-bottom:10px; }
      .footer { font-size:0.75rem; color:#6fb9c8; opacity:0.7; margin-top:12px; text-align:center; }
    </style>
  </head>
  <body>
    <div class="card p-4">
      <div class="mb-3 text-center">
        <div class="logo">ADAPTIVE-THREAT</div>
        <div class="small-muted">Secure dashboard — enter credentials</div>
      </div>
      {% if error %}
      <div class="error">{{ error }}</div>
      {% endif %}
      <form method="post" action="/login">
        <div class="mb-2">
          <label class="form-label small-muted">Username</label>
          <input name="username" class="form-control" autofocus required />
        </div>
        <div class="mb-3">
          <label class="form-label small-muted">Password</label>
          <input name="password" type="password" class="form-control" required />
        </div>
        <button class="btn btn-primary w-100" type="submit">Sign in</button>
        <div class="hint">Tip: default demo creds are in <code>src/auth_config.json</code></div>
      </form>
      <div class="footer">Safe Mode: no firewall changes by default</div>
    </div>
  </body>
</html>
